#ifndef SYSTEM_TIMERMANAGER_H
#define SYSTEM_TIMERMANAGER_H

////////////////////////////////////////////////////////////
// Headers
////////////////////////////////////////////////////////////
#include <System/Timer.h>

////////////////////////////////////////////////////////////
// Add a timer to the manager (with a pointer)
////////////////////////////////////////////////////////////
void TimerAddPtr(struct Timer * Data);

////////////////////////////////////////////////////////////
// Add a timer to the manager
////////////////////////////////////////////////////////////
struct Timer * TimerAddProc(UInt32 Interval, void (*Func)(struct Timer * T));

#endif // SYSTEM_TIMERMANAGER_H
